# Выгружается из базы при каждом переобучении — версионировать нечего.
feedback.csv

# Промежуточные артефакты переобучения: остаются только при неудачной попытке,
# для разбора.
model.candidate.json
model.candidate.report.json

__pycache__/
