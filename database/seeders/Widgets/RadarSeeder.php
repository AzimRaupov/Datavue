<?php

namespace Database\Seeders\Widgets;

class RadarSeeder extends WidgetFamilySeeder
{
    protected function family(): array
    {
        return [
            'name' => 'radar',
            'description' => 'Сравнение нескольких сущностей сразу по нескольким (от 3) числовым измерениям, когда важна '
                .'форма профиля целиком: сравнить 2-3 товара по 5 характеристикам, отделы по набору KPI. '
                .'НЕ используйте для одной метрики, одной сущности или динамики во времени — это не его форма данных.',

            'scheme' => [
                'series' => [
                    ['name' => 'string', 'data' => [80, 50, 65]],
                ],
                'categories' => ['string', 'string', 'string'],
            ],

            'scheme_description' => <<<TEXT
series — массив рядов, каждый ряд это одна сравниваемая сущность:
- name — название сущности (строка);
- data — массив чисел по осям.

categories — массив названий осей (измерений).

Правила:
- осей должно быть минимум 3, иначе фигуры не получится;
- длина каждого data равна длине categories, порядок значений совпадает с порядком осей;
- все ряды одинаковой длины;
- рядов разумно 2-4: больше — паутина становится нечитаемой;
- измерения должны быть сопоставимы по масштабу (проценты, баллы, нормированные
  величины). Если одна ось в миллионах, а другая в единицах, радар бесполезен —
  возьмите "bar".
TEXT,
        ];
    }

    protected function types(): array
    {
        return [
            [
                'name' => 'radar',
                'title' => 'Контур',
                'description' => 'Линии без заливки. Выбор по умолчанию, когда сравниваются 3-4 сущности и заливка '
                    .'перекрывала бы соседние контуры.',
                'options' => ['chartType' => 'radar', 'filled' => false],
                'is_default' => true,
            ],
            [
                'name' => 'filled',
                'title' => 'С заливкой',
                'description' => 'Полупрозрачная заливка внутри контура. Берите для 1-2 сущностей: заливка делает '
                    .'форму профиля заметнее.',
                'options' => ['chartType' => 'radar', 'filled' => true],
            ],
            [
                'name' => 'polar-area',
                'title' => 'Полярные секторы',
                'description' => 'Секторы круга разной длины по одной метрике. Берите, когда сущность одна, а измерений '
                    .'много — это читается лучше одинокого контура. Форма данных здесь другая: плоский список значений.',
                'options' => ['chartType' => 'polarArea'],
                'scheme' => [
                    'series' => [21, 15, 9],
                    'labels' => ['string', 'string', 'string'],
                ],
                'scheme_description' => <<<TEXT
series — массив чисел, по одному значению на измерение.
labels — массив названий измерений (строки).

Правила:
- длина labels равна длине series, series[i] относится к labels[i];
- значения неотрицательные;
- это форма для ОДНОЙ сущности; для сравнения нескольких берите тип "radar".
TEXT,
            ],
        ];
    }
}
