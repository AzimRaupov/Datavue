<?php

namespace Database\Seeders\Widgets;

class ComboSeeder extends WidgetFamilySeeder
{
    protected function family(): array
    {
        return [
            'name' => 'combo',
            'description' => 'Две метрики разной природы или разного масштаба на одном графике: столбцы для объёма и '
                .'линия для относительной величины. Классика — выручка (столбцы) и маржинальность в % (линия), '
                .'количество заказов и средний чек. Берите, когда метрики нужно видеть вместе, но на одной шкале '
                .'одна из них была бы незаметна.',

            'scheme' => [
                'series' => [
                    ['name' => 'string', 'kind' => 'column', 'data' => [12, 34]],
                    ['name' => 'string', 'kind' => 'line', 'data' => [5.5, 7.1]],
                ],
                'categories' => ['string', 'string'],
            ],

            'scheme_description' => <<<TEXT
series — массив рядов. Для каждого элемента:
- name — название метрики (строка);
- kind — как рисовать этот ряд: "column" (столбцы) или "line" (линия);
- data — массив чисел (int или float).

categories — массив подписей оси X (строки), обычно периоды времени или категории.

Правила:
- рядов ровно 2 (реже 3): столбцы для абсолютных величин, линия для процентов,
  средних и коэффициентов;
- минимум один ряд с kind="column" и минимум один с kind="line" — иначе
  это обычный "bar" или "line", возьмите их;
- длина каждого data равна длине categories;
- у ряда с линией своя ось справа, поэтому масштабы могут отличаться на порядки.
TEXT,
        ];
    }

    protected function types(): array
    {
        return [
            [
                'name' => 'bar-line',
                'title' => 'Столбцы и линия',
                'description' => 'Объём столбцами, вторая метрика линией на правой оси. Выбор по умолчанию.',
                'options' => ['columnKind' => 'column', 'lineKind' => 'line'],
                'is_default' => true,
            ],
            [
                'name' => 'area-line',
                'title' => 'Область и линия',
                'description' => 'Первая метрика заливкой вместо столбцов. Берите для непрерывных величин во времени, '
                    .'где столбцы дробят картину (ежедневные данные за длинный период).',
                'options' => ['columnKind' => 'area', 'lineKind' => 'line'],
            ],
        ];
    }
}
