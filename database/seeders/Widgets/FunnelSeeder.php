<?php

namespace Database\Seeders\Widgets;

class FunnelSeeder extends WidgetFamilySeeder
{
    protected function family(): array
    {
        return [
            'name' => 'funnel',
            'description' => 'Воронка: последовательные этапы процесса с убыванием количества на каждом шаге. '
                .'Заявка → счёт → оплата → отгрузка, визит → корзина → заказ, кандидат → интервью → оффер. '
                .'Берите ТОЛЬКО когда этапы идут строго друг за другом и каждый следующий — подмножество предыдущего. '
                .'Для независимых категорий это "bar".',

            'scheme' => [
                'labels' => ['string', 'string'],
                'series' => [1000, 420],
            ],

            'scheme_description' => <<<TEXT
labels — массив названий этапов (строки) в порядке прохождения.
series — массив количеств на каждом этапе (int или float).

Правила:
- длина labels равна длине series, series[i] относится к labels[i];
- порядок строго от первого этапа к последнему;
- значения должны убывать (или хотя бы не расти): воронка, где следующий этап
  больше предыдущего, означает ошибку в запросе;
- разумное число этапов — от 3 до 7.
TEXT,
        ];
    }

    protected function types(): array
    {
        return [
            [
                'name' => 'funnel',
                'title' => 'Воронка',
                'description' => 'Полосы, сужающиеся сверху вниз, с подписью этапа. Выбор по умолчанию.',
                'options' => ['shape' => 'funnel'],
                'is_default' => true,
            ],
            [
                'name' => 'pyramid',
                'title' => 'Пирамида',
                'description' => 'Та же воронка, перевёрнутая вершиной вверх. Берите, когда логика идёт от массового '
                    .'основания к вершине (все клиенты → активные → VIP).',
                'options' => ['shape' => 'pyramid'],
            ],
        ];
    }
}
