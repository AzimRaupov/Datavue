<?php

namespace Database\Seeders\Widgets;

class HeatmapSeeder extends WidgetFamilySeeder
{
    protected function family(): array
    {
        return [
            'name' => 'heatmap',
            'description' => 'Матрица: значение метрики на пересечении ДВУХ измерений, закодированное цветом. '
                .'Классические задачи — активность по дням недели и часам, продажи по регионам и месяцам, '
                .'загрузка по сотрудникам и неделям. Берите, когда разрезов два и важно найти горячие зоны, '
                .'а не точные числа.',

            'scheme' => [
                'series' => [
                    [
                        'name' => 'string',
                        'data' => [
                            ['x' => 'string', 'y' => 12],
                        ],
                    ],
                ],
            ],

            'scheme_description' => <<<TEXT
series — массив строк матрицы (первое измерение). Для каждого элемента:
- name — подпись строки (строка), например "Понедельник";
- data — массив ячеек этой строки, каждая ячейка это объект:
  - x — подпись колонки (строка), например "10:00";
  - y — значение метрики в ячейке (int или float).

Правила:
- набор x должен быть ОДИНАКОВЫМ и идти в одном порядке во всех рядах —
  иначе колонки матрицы разъедутся;
- пропущенные комбинации заполняйте нулём, а не пропускайте;
- разумный размер матрицы — до ~15 строк и ~30 колонок;
- метрика должна быть сопоставима между ячейками (одна и та же величина).
TEXT,
        ];
    }

    protected function types(): array
    {
        return [
            [
                'name' => 'gradient',
                'title' => 'Плавный градиент',
                'description' => 'Цвет меняется непрерывно от минимума к максимуму. Выбор по умолчанию: показывает, '
                    .'насколько одна ячейка «горячее» другой.',
                'options' => ['chartType' => 'heatmap', 'discrete' => false],
                'is_default' => true,
            ],
            [
                'name' => 'discrete',
                'title' => 'Ступени',
                'description' => 'Значения разложены по диапазонам с отдельным цветом на каждый. Берите, когда важны '
                    .'пороги («мало / норма / много»), а не плавная разница.',
                'options' => ['chartType' => 'heatmap', 'discrete' => true],
            ],
        ];
    }
}
