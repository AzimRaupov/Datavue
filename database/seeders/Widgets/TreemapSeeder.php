<?php

namespace Database\Seeders\Widgets;

class TreemapSeeder extends WidgetFamilySeeder
{
    protected function family(): array
    {
        return [
            'name' => 'treemap',
            'description' => 'Доли категорий прямоугольниками: площадь блока пропорциональна величине. '
                .'Берите вместо "pie", когда категорий много (10-50) и круговая диаграмма превратилась бы в кашу, '
                .'но структура целого всё равно важнее точных значений.',

            'scheme' => [
                'series' => [
                    [
                        'data' => [
                            ['x' => 'string', 'y' => 12],
                        ],
                    ],
                ],
            ],

            'scheme_description' => <<<TEXT
series — массив с ОДНИМ элементом. У него:
- data — массив блоков, каждый блок это объект:
  - x — название категории (строка);
  - y — величина категории (int или float).

Правила:
- значения y неотрицательные: площадь отрицательного размера не рисуется;
- сортируйте data по убыванию y;
- разумный объём — до ~50 блоков, мелкие лучше свернуть в "Прочее";
- несколько элементов в series не поддерживаются — сложите всё в один.
TEXT,
        ];
    }

    protected function types(): array
    {
        return [
            [
                'name' => 'treemap',
                'title' => 'Одним цветом',
                'description' => 'Блоки в одной цветовой гамме, оттенок по величине. Выбор по умолчанию: подчёркивает '
                    .'размер, а не принадлежность.',
                'options' => ['chartType' => 'treemap', 'distributed' => false],
                'is_default' => true,
            ],
            [
                'name' => 'distributed',
                'title' => 'Разными цветами',
                'description' => 'Каждый блок своего цвета. Берите, когда категории нужно различать между собой '
                    .'(бренды, направления), а блоков не больше ~12.',
                'options' => ['chartType' => 'treemap', 'distributed' => true],
            ],
        ];
    }
}
